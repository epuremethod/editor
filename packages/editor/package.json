{
  "name": "@epure/editor",
  "version": "0.0.0",
  "description": "The core of a block editor over sections of markdown: the types, the pure model, the notation and the storage port. Depends on nothing.",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "res:build": "rescript",
    "res:dev": "rescript watch",
    "format": "rescript format",
    "pretest": "pnpm res:build",
    "test": "vitest run"
  },
  "devDependencies": {
    "@epure/vitest": "2.0.0",
    "@rescript/runtime": "^12.3.0",
    "rescript": "^12.3.0",
    "vitest": "^3.2.4"
  },
  "peerDependencies": {
    "@rescript/runtime": "^12.3.0"
  }
}
